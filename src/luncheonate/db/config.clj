(ns luncheonate.db.config
  (:use korma.db))

;(def config
  ;(if (= (System/getenv "RING_ENV") "production")
          ;{}
          ;{:subprotocol "postgresql"
           ;:subname "//localhost:5432/luncheonate"
           ;:user "luncheonate"
           ;:password ""}))

;(defdb db (postgres config))
